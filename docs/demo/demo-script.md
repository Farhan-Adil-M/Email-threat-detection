# SENTINEL Demo Script (2–4 minutes)

1. Open the dashboard.
2. Upload `backend/data/fixtures/bec.eml` through `/docs`.
3. Analyze the created case.
4. Show deterministic findings and their score contributions.
5. Run ML analysis and show model version plus limitations.
6. Run risk assessment and open the explanation endpoint.
7. Show graph nodes/edges and ledger verification.
8. Generate `/report.json`.

All fixtures are synthetic. Provider results are labeled `disabled` or `fixture` unless live mode is explicitly enabled.
